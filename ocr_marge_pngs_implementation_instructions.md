# `marge_pngs.py` OCR対応化 実装指示書

## 1. 目的

現在の `marge_pngs.py` は、Kindle for PC から取得した複数の PNG 画像を 1 冊分の PDF へ結合するスクリプトです。

これを改修し、

**Kindle スクリーンショット → OCR 処理 → 検索可能 PDF 生成**

までを `marge_pngs.py` だけで完結できるようにしてください。

最終成果物は、見た目としては元の Kindle スクリーンショットを維持しつつ、PDF 内部に OCR 済みテキストレイヤーが埋め込まれており、以下を実現できる状態を目標とします。

- PDF 内文字検索
- テキスト選択
- ChatGPT 等への PDF アップロード後の本文参照
- 読書プロジェクト内での章横断質問

---

## 2. 現在の構成

現在の想定処理フローは以下です。

```text
Kindle for PC
    ↓
kindless.py
    ↓
001.png
002.png
003.png
...
    ↓
marge_pngs.py
    ↓
書籍名.pdf
```

`kindless.py` は Kindle 画面をキャプチャし、ページ番号付き画像として保存します。

`marge_pngs.py` は各書籍フォルダを走査し、PNG を自然順で並べて PDF 化しています。

今後は以下に変更します。

```text
Kindle for PC
    ↓
kindless.py
    ↓
001.png
002.png
003.png
...
    ↓
marge_pngs.py
    ↓
OCR
    ↓
検索可能 PDF
    ↓
書籍名.pdf
```

---

## 3. 基本方針

実装はなるべくシンプルにしてください。

このツールの利用者は非エンジニアであり、コーディング経験もありません。したがって、以下を優先してください。

- コマンドが複雑にならない
- 設定項目を増やしすぎない
- エラー内容が日本語で理解できる
- 実行方法が現在と大きく変わらない
- 既存の Kindle キャプチャ処理を壊さない
- 過剰な抽象化や大規模リファクタリングを避ける

理想的には、今まで通り以下だけで OCR 済み PDF が作成されるようにしてください。

```bash
python marge_pngs.py
```

---

## 4. OCR方式

OCR エンジンには原則として **Tesseract OCR** を使用してください。

Python 側では `pytesseract` を利用します。

主対象は日本語技術書です。英数字やコードも混在するため、言語設定は以下を第一候補として検討してください。

```text
jpn+eng
```

ただし、実際の OCR 結果や導入の簡潔さを踏まえて `jpn` の方が妥当であれば、その理由を説明したうえで採用して構いません。

---

## 5. PDFの要件

生成される PDF は、単なる OCR テキストだけの PDF ではなく、

**元画像 + OCR テキストレイヤー**

の形式にしてください。

つまり見た目は元の Kindle ページを保ちます。

以下を満たしてください。

- 元画像のレイアウトを維持
- 図やコードブロックを維持
- ページ番号を維持
- OCR 文字列を透明なテキストレイヤーとして持つ
- PDF ビューアで `Ctrl + F` 検索可能
- テキスト選択可能
- ChatGPT へアップロードした際、本文解析しやすい PDF になる
- 全ページが正しい順序で含まれる

---

## 6. 推奨実装方式

第一候補として `pytesseract.image_to_pdf_or_hocr()` を検討してください。

例:

```python
pdf_bytes = pytesseract.image_to_pdf_or_hocr(
    image,
    extension="pdf",
    lang="jpn+eng",
)
```

各ページについて OCR 済み PDF を生成し、その後 1 冊分として結合する構成を想定しています。

PDF 結合には `pypdf` を使用して構いません。

想定 Python パッケージ:

```bash
pip install pytesseract pypdf pillow natsort
```

ただし、よりシンプルかつ Windows で安定する実装方法がある場合は変更して構いません。

`OCRmyPDF` 等の別方式を採用する場合は、以下を満たしてください。

- Windows での導入が過度に複雑にならない
- 非エンジニアでも再現できる
- 追加依存を増やす理由が明確
- 実装後に導入手順を日本語で説明できる

---

## 7. ファイル構成

現在のフォルダ構成を基本的に維持してください。

例:

```text
C:\kindle_ss\
 ├─ 書籍A\
 │   ├─ 001.png
 │   ├─ 002.png
 │   ├─ 003.png
 │   └─ 書籍A.pdf
 │
 └─ 書籍B\
     ├─ 001.png
     ├─ 002.png
     └─ 書籍B.pdf
```

生成 PDF 名は現在と同様、以下としてください。

```text
{folder.name}.pdf
```

---

## 8. 処理対象フォルダ

現在の以下の保存先を基本的に維持してください。

```python
base = Path("C:/kindle_ss")
```

可能であれば、コード先頭に設定値としてまとめてください。

例:

```python
BASE_DIR = Path("C:/kindle_ss")
```

大規模な設定ファイル追加は不要です。

---

## 9. ページ順

以下のようなファイル名が正しい順番になるようにしてください。

```text
001.png
002.png
003.png
...
010.png
```

`natsort` の利用は継続して構いません。

ページ順が崩れないことは必須です。

---

## 10. OCR処理単位とメモリ使用量

本 1 冊を一度に全てメモリへ展開しないでください。

数百ページになることがあります。

原則として以下の逐次処理にしてください。

```text
1ページ読み込み
↓
OCR
↓
PDFページ生成
↓
結合先へ追加
↓
次ページ
```

数百ページでもメモリ不足になりにくい設計にしてください。

---

## 11. 中間ファイル

可能なら、大量の一時 PDF をディスクへ残さない方法を優先してください。

例えば `BytesIO` を利用し、

```text
PNG
↓
OCR PDF bytes
↓
PdfReader
↓
PdfWriterへ追加
```

のような方式を検討してください。

ただし、安定性が下がる場合は一時ディレクトリを使っても構いません。

一時ファイルを使う場合は、正常終了時だけでなくエラー時も可能な範囲で後片付けしてください。

---

## 12. 既存PDFがある場合

現在の思想を基本的に維持してください。

```python
if out.exists():
    return 0, f"SKIP: PDF既存 {out.name}"
```

つまり、`書籍名.pdf` が存在する場合は勝手に上書きしないでください。

表示例:

```text
SKIP: PDF既存 Codexではじめるエージェンティックコーディング.pdf
```

---

## 13. OCR進捗表示

OCR は現在の PDF 生成より時間がかかるため、進捗が分かるようにしてください。

例:

```text
========================================
Codexではじめるエージェンティックコーディング
========================================

312 PNG files found.

OCR 1/312 : 001.png
OCR 2/312 : 002.png
OCR 3/312 : 003.png
...
OCR 312/312 : 312.png

DONE:
Codexではじめるエージェンティックコーディング.pdf

Pages: 312
Elapsed: 08:32
```

標準 `print()` で構いません。

外部ライブラリ `tqdm` は必須ではありません。

---

## 14. エラーハンドリング

最低限、以下を検出してください。

### 14.1 Tesseract が見つからない

例:

```text
ERROR:
Tesseract OCR が見つかりません。

Tesseract をインストールし、
PATH が設定されているか確認してください。
```

### 14.2 日本語 OCR データがない

例:

```text
ERROR:
Tesseract の日本語言語データ「jpn」が見つかりません。
```

`jpn+eng` を採用する場合は、必要言語データを確認してください。

### 14.3 PNG が存在しない

現在と同様、以下で構いません。

```text
SKIP: 画像なし
```

### 14.4 特定ページだけ OCR に失敗

例:

```text
ERROR: 153.png の OCR に失敗しました
```

ページ欠落は後から気付きにくいため、原則として **本全体を失敗扱い** としてください。

不完全な PDF を正常完了扱いにしないでください。

---

## 15. OCR精度

主対象は以下です。

- 日本語横書き
- 技術書
- コード
- 英単語
- 見出し
- 図表

初期実装では、複雑な画像前処理は不要です。

まずは元 PNG をそのまま Tesseract へ渡してください。

以下は初期実装では不要です。

- 二値化
- 傾き補正
- ノイズ除去
- AI OCR
- レイアウト解析
- 図表認識
- 複雑な前処理パイプライン

精度に問題が出た場合に、後から改善しやすい構成にしてください。

---

## 16. Tesseract設定

Windows 環境を想定しています。

通常の PATH 登録済み環境では、`pytesseract.pytesseract.tesseract_cmd` を明示設定しなくても動く設計を優先してください。

必要であれば、以下のような一般的なインストール先の自動検出を検討して構いません。

```text
C:\Program Files\Tesseract-OCR\tesseract.exe
```

ただし過剰な自動検出ロジックは不要です。

---

## 17. PDFサイズ

OCR 対応によって PDF 容量が極端に増えないよう注意してください。

ただし、画像品質を大きく落として容量削減することはしないでください。

今回の優先順位は以下です。

```text
可読性 > PDF容量
```

---

## 18. 既存コードへの影響

今回の主対象は以下です。

```text
marge_pngs.py
```

原則として、以下は必要がない限り変更しないでください。

```text
kindless.py
WindowInfo.py
wxdialog.py
dataclass.py
kindless.ini
```

どうしても変更する場合は、理由を実装結果に記載してください。

---

# 19. 不要ファイル整理方針

現状のリポジトリには、過去の実装や現在の実行フローでは使われていないファイルが残っている可能性があります。

今回の実装と合わせて、不要ファイルの整理も行ってください。

ただし、**削除は禁止** とします。

## 19.1 `trash-can` フォルダを作る

リポジトリ直下に以下を作成してください。

```text
trash-can/
```

現在不要と判断できるファイルは、このフォルダへ移動してください。

例:

```text
repository-root/
├─ kindless.py
├─ marge_pngs.py
├─ dataclass.py
├─ WindowInfo.py
├─ wxdialog.py
├─ kindless.ini
├─ README.md
└─ trash-can/
   └─ 過去の不要ファイル
```

## 19.2 移動前の確認

ファイルを `trash-can` へ移動する前に、必ず以下を確認してください。

- Python の `import` で参照されていないか
- 他のスクリプトから呼び出されていないか
- README の現行手順で使われていないか
- 設定ファイルから参照されていないか
- 現在の実行フローに必要でないか
- テストや補助スクリプトとして有効な役割が残っていないか

**未使用であることを十分に確認できたファイルだけ移動してください。**

判断が曖昧なファイルは移動しないでください。

## 19.3 既存ファイルを勝手に削除しない

以下は禁止します。

```text
rm
del
unlink
完全削除
```

不要と判断しても、必ず `trash-can` へ移動してください。

## 19.4 `trash-can` は実行対象外

`trash-can` 配下の Python ファイル等は、通常実行・import・テスト対象に含めないでください。

必要に応じて `.gitignore` の変更を検討しても構いませんが、`trash-can` 自体を Git 管理から外す必要はありません。

「過去ファイルを一時退避して履歴として残す」という目的を優先してください。

## 19.5 移動結果の報告

実装完了時に、以下を報告してください。

```text
trash-can へ移動したファイル
- ファイル名
- 移動した理由
- 現在のコードから参照されていないと判断した根拠
```

ファイルを 1 つも移動しなかった場合も、

```text
確認したが、現時点では安全に不要と判断できるファイルはなかった
```

と明記してください。

---

## 20. README更新

実装後、`README.md` も更新してください。

README には最低限、以下を追加・修正してください。

### 必要ソフト

```text
Tesseract OCR
```

### Pythonライブラリ

実装に合わせて記載してください。

例:

```bash
pip install pytesseract pypdf pillow natsort
```

### OCR言語

日本語データ `jpn` が必要であることを記載してください。

英語も利用する場合は `eng` についても記載してください。

### 実行方法

```bash
python marge_pngs.py
```

### 出力

```text
検索可能 PDF
```

が生成されることを明記してください。

### `trash-can`

不要ファイルは削除せず、`trash-can` に退避する方針も簡潔に記載してください。

---

## 21. READMEの古い記述も整理

README 内に、現在の実装と矛盾する説明がある場合は修正してください。

特に、以下のような古い説明が残っていないか確認してください。

```text
キャプチャ画像から PDF を作成する工程は本リポジトリには含まれていない
```

現在は `marge_pngs.py` が存在するため、実態に合わせて修正してください。

README 全体を現状の実行フローと矛盾しない状態にしてください。

---

## 22. requirements.txt の扱い

リポジトリ内に `requirements.txt` が存在するか確認してください。

### 存在する場合

今回追加した依存ライブラリを追記してください。

### 存在しない場合

必要であれば新規作成して構いません。

ただし、依存関係が少なく README の `pip install` だけで十分と判断した場合は、無理に作成しなくても構いません。

判断理由を実装後に説明してください。

---

## 23. CLIは複雑にしない

今回は以下のような CLI オプションは原則不要です。

```text
--input
--output
--language
--dpi
--force
```

最初は以下だけで動作させてください。

```bash
python marge_pngs.py
```

---

## 24. ログ出力

ログファイル作成は不要です。

コンソール出力のみで構いません。

ただし、以下が判断できるようにしてください。

- どの本を処理しているか
- 何ページ目を処理しているか
- 何が失敗したか
- 最終的に成功したか
- 何ページ処理したか
- 処理時間

---

## 25. コード品質

以下を意識してください。

- 1関数が長くなりすぎない
- 型ヒントを可能な範囲で利用
- 処理ごとに関数を分離
- コメントは必要な箇所だけ
- 過剰なクラス化をしない
- 初心者でも追いやすい構造
- 不要な抽象化をしない
- 既存の簡潔さをできるだけ維持する

想定例:

```python
def pngs_in(...)
def check_tesseract(...)
def ocr_page(...)
def merge_folder(...)
def main(...)
```

程度で十分です。

---

## 26. 想定される完成形

最終的に以下を実行すると、

```bash
python marge_pngs.py
```

以下のように動作することをイメージしています。

```text
========================================
Codexではじめるエージェンティックコーディング
========================================

312 PNG files found.

OCR 1/312 : 001.png
OCR 2/312 : 002.png
OCR 3/312 : 003.png

...

OCR 312/312 : 312.png

DONE:
Codexではじめるエージェンティックコーディング.pdf

Pages: 312
Elapsed: 08:32
```

---

## 27. 受入条件

以下をすべて満たしたら完成としてください。

### 必須

- `python marge_pngs.py` だけで実行できる
- `C:/kindle_ss` 以下の各書籍フォルダを処理できる
- PNG をページ番号順に処理する
- 日本語 OCR が行われる
- 元ページ画像が PDF 内に維持される
- OCR テキストレイヤーが埋め込まれる
- PDF で文字検索できる
- PDF で文字選択できる
- 数百ページでも処理できる
- 既存 PDF は上書きしない
- OCR 進捗が表示される
- エラー内容が分かりやすい
- README を更新する
- 不要ファイルを削除しない
- 不要と確定したファイルは `trash-can` へ移動する
- `trash-can` への移動理由を報告する

### 動作確認

最低限 3〜5 ページ程度のテスト用 PNG フォルダを使って、

```text
PNG → OCR → PDF
```

を確認してください。

生成 PDF について、以下を確認してください。

1. PDF が開ける
2. 全ページ存在する
3. `Ctrl + F` で日本語を検索できる
4. 本文をマウスで選択できる
5. 元画像の見た目が大きく崩れていない

---

## 28. 実装後にClaude Codeから報告してほしい内容

実装後はコード変更だけで終了せず、以下を説明してください。

```text
1. 何を変更したか
2. OCR がどのような仕組みで動くか
3. 新しく必要になったソフト・Python ライブラリ
4. 私が Windows 側で行う必要がある設定
5. 実行コマンド
6. 動作確認方法
7. 既知の制約
8. trash-can へ移動したファイル一覧
9. 各ファイルを移動した理由
10. 変更したファイル一覧
```

私は非エンジニアなので、専門用語だけで説明せず、操作ベースで説明してください。

---

## 29. 実装前に確認してほしいこと

実装を始める前にリポジトリ内を確認し、最低限以下を把握してください。

- 現在の `marge_pngs.py`
- `kindless.py`
- `README.md`
- `WindowInfo.py`
- `wxdialog.py`
- `dataclass.py`
- `kindless.ini`
- `requirements.txt` の有無
- `.gitignore`
- Python バージョン前提
- 各ファイル間の import / 参照関係
- 現在使用されていない可能性があるファイル

そのうえで、

**既存設計を壊さない最小変更**

で実装してください。

途中で私への確認が必須な重大な設計判断がなければ、質問せずそのまま以下まで進めてください。

```text
調査
↓
不要ファイル候補の確認
↓
trash-can への安全な退避
↓
OCR 実装
↓
テスト
↓
README 更新
↓
結果報告
```

---


## 30. 起動ボタンの追加

Python コマンドを毎回ターミナルから入力しなくても使えるように、**Windows からダブルクリックで起動できるボタン／ランチャー**を用意してください。

利用者は非エンジニアのため、日常利用ではできるだけコマンド入力を不要にしてください。

### 30.1 必須要件

最低限、以下の 3 つの処理を簡単に起動できるようにしてください。

1. Kindle を右方向にページ送りしながらキャプチャ開始
2. Kindle を左方向にページ送りしながらキャプチャ開始
3. PNG から OCR 対応 PDF を生成

起動方法は、以下のいずれかを第一候補としてください。

- `.bat`
- `.cmd`
- PowerShell ラッパー
- シンプルな Python GUI

ただし、依存関係や導入手順を増やしすぎないでください。

**Windows でダブルクリックして使えることを優先**してください。

---

## 31. Kindle のページ送り方向への対応

Kindle 書籍には、ページ送り方向が異なるものがあります。

- 右方向へ進む書籍
- 左方向へ進む書籍

現在は `kindless.ini` の `nextpage_key` でページ送りキーを設定できる構成ですが、毎回設定ファイルを書き換える運用にはしないでください。

### 31.1 希望する操作

ユーザーが実行時に迷わないよう、例えば以下のような 2 つの起動ボタンを用意してください。

```text
Kindleキャプチャ_右へ進む.bat
Kindleキャプチャ_左へ進む.bat
```

または同等の分かりやすい名前にしてください。

例えば:

```text
capture_right.bat
capture_left.bat
```

よりも、日本語名の方が利用者には分かりやすいため、Windows 上で問題がなければ日本語ファイル名を優先してください。

### 31.2 実装方針

起動ボタンごとに `kindless.py` へページ送り方向を渡せるようにしてください。

実装方法は以下のいずれかを検討してください。

#### 第一候補: CLI 引数

例:

```bash
python kindless.py --next-page right
python kindless.py --next-page left
```

または、

```bash
python kindless.py --direction right
python kindless.py --direction left
```

のように、意味が分かりやすい形式にしてください。

そのうえで各 `.bat` ファイルから対応するコマンドを呼び出します。

#### 別案: 設定ファイルを方向別に分ける

例:

```text
kindless_right.ini
kindless_left.ini
```

ただし設定ファイルが重複し、管理が煩雑になるため、**可能なら CLI 引数方式を優先**してください。

### 31.3 既存設定との互換性

現在の `kindless.ini` の `nextpage_key` は残して構いません。

CLI 引数が指定された場合のみ一時的に上書きし、指定がない場合は従来どおり `kindless.ini` の値を使う方式が望ましいです。

つまり、

```text
python kindless.py
```

は従来動作を維持し、

```text
python kindless.py --direction right
python kindless.py --direction left
```

では実行時だけページ送り方向を切り替える構成にしてください。

既存利用方法を壊さないことを優先してください。

---

## 32. ページ送り方向とキーの対応

現在の Kindle for PC の動作を確認したうえで、`right` / `left` が実際にどのキー入力に対応するかを実装してください。

例えば、

```text
--direction right
    → pag.press("right")

--direction left
    → pag.press("left")
```

という対応を想定しています。

ただし、既存コードでは `nextpage_key` を利用しているため、実際の Kindle for PC の挙動と現在の設定を確認したうえで決定してください。

ここでの `right` / `left` は、**「どちらの矢印キーを押すか」** がユーザーから見て明確になるようにしてください。

曖昧な命名は避けてください。

必要であれば起動ファイル名を、

```text
Kindleキャプチャ_右矢印.bat
Kindleキャプチャ_左矢印.bat
```

としても構いません。

---

## 33. OCR PDF 用の起動ボタン

OCR PDF 生成についても、ダブルクリックで実行できる起動ファイルを用意してください。

例:

```text
OCR_PDF作成.bat
```

内部では以下を実行します。

```bash
python marge_pngs.py
```

実行終了後、結果を確認できるようにウィンドウがすぐ閉じない構成にしてください。

例:

```bat
@echo off
python marge_pngs.py
pause
```

ただし、仮想環境を使う構成など、リポジトリの実態に合う方法があれば適切に調整してください。

---

## 34. 起動ボタンの配置

ユーザーが迷わないように、起動用ファイルは原則として **リポジトリ直下** に置いてください。

想定:

```text
repository-root/
├─ Kindleキャプチャ_右矢印.bat
├─ Kindleキャプチャ_左矢印.bat
├─ OCR_PDF作成.bat
├─ kindless.py
├─ marge_pngs.py
├─ kindless.ini
├─ README.md
└─ trash-can/
```

ファイル数が増えすぎる場合は `launcher/` 等のサブフォルダ案も検討して構いませんが、日常利用でアクセスしやすいことを優先してください。

---

## 35. 起動ボタンのエラー表示

ダブルクリック起動時にエラーが発生しても、コンソールが即座に閉じて内容が読めない状態にしないでください。

少なくとも以下のいずれかを実装してください。

- 最後に `pause`
- エラー時のみ `pause`
- Python 側で分かりやすいエラーメッセージを表示して Enter 待ち

利用者がエラー内容をスクリーンショットして AI に相談できる状態を重視してください。

---

## 36. READMEへの起動ボタン説明追加

README の先頭付近に、**普段使いの操作方法**として起動ボタンを分かりやすく記載してください。

例:

```text
## 普段の使い方

### 右矢印でページ送りする本
`Kindleキャプチャ_右矢印.bat` をダブルクリック

### 左矢印でページ送りする本
`Kindleキャプチャ_左矢印.bat` をダブルクリック

### OCR済みPDFを作る
`OCR_PDF作成.bat` をダブルクリック
```

非エンジニアが README を開いたとき、Python コマンドより先にこの説明が目に入る構成を優先してください。

---

## 37. 起動ボタンに関する受入条件

以下を追加の受入条件とします。

- Windows 上でダブルクリックしてキャプチャを開始できる
- 右矢印用・左矢印用の 2 種類を選べる
- 毎回 `kindless.ini` を書き換える必要がない
- CLI からの従来実行方法も維持される
- OCR PDF 作成もダブルクリックで実行できる
- 実行終了・エラー時にウィンドウが即座に閉じない
- README に 3 つの起動方法が明記される
- 起動ファイル自体は `trash-can` の整理対象にしない


# Claude Codeへの最初の依頼文

以下の文と、この指示書をセットで渡してください。

> 添付の実装指示書に従って、このリポジトリの `marge_pngs.py` を OCR 対応してください。  
> まず既存コード、README、設定ファイル、依存関係を確認し、現在の動作を把握したうえで、既存機能を壊さない最小変更で実装してください。  
>  
> あわせて、Windows からダブルクリックで使える起動ボタンも作成してください。Kindle キャプチャは右矢印でページ送りする本と左矢印でページ送りする本の両方に対応し、原則として 2 つの起動ファイルを用意してください。OCR PDF 作成用の起動ファイルも用意してください。
>  
> あわせて、現状不要になっているファイルがないか確認してください。不要と十分に判断できるファイルは削除せず、リポジトリ直下に `trash-can` フォルダを作成してそこへ移動してください。参照関係や import の確認が取れないもの、判断が曖昧なものは移動しないでください。  
>  
> 私は非エンジニアなので、実装後の説明は「何をしたか」「私が何をインストールする必要があるか」「今後どう実行するか」を操作ベースで説明してください。  
>  
> 重大な設計上の問題がなければ途中確認は不要です。調査、不要ファイル整理、実装、簡単な動作確認、README 更新、結果報告まで進めてください。
